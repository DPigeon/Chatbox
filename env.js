/* The environment variables used for development & production
 */

const SERVER_URL = "0.0.0.0"; // Will change depending on process.env later
const SERVER_PORT = 32000;

module.exports = { SERVER_URL, SERVER_PORT };
